name: Setup
description: Installe Node et les dépendances npm avec cache

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm

    - name: Install
      run: npm ci
      shell: bash
